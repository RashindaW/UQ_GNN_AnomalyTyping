torch>=2.1
torch-geometric>=2.5
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
pytz>=2023.3
openpyxl>=3.1
pyarrow>=14         # detect.py per-query payload as Parquet (CSV fallback if missing)

# DualSTGF additional deps (vendored under dualstgf/)
pyreadr>=0.5.0     # PRONTO raw-data loader uses RData files
xlrd>=2.0.1        # legacy .xls support for some dataset variants
seaborn>=0.12      # plots in dualstgf/evaluation
plotly>=5.18       # interactive dashboards in dualstgf/evaluation
tqdm>=4.65         # progress bars in dualstgf training loop

# torch-scatter / torch-sparse / torch-cluster / torch-spline-conv are
# OPTIONAL in PyG 2.x; install only if you hit a "not implemented" error.
# Match the wheel index to your torch version, e.g.:
#   pip install torch-scatter -f https://data.pyg.org/whl/torch-2.9.0+cu128.html
